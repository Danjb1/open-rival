#include "pch.h"
#include <iostream>

int main() {
    /*
     * TODO:
     *  - Load a texture definition
     *  - For each item, load the image, add it to the texture
     *  - Start a new row once we hit the max image size
     */
}
